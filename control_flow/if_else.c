#include <stdio.h>

int main() {
    int num = 1;
    int error_code = 400;

    // if(num == 2) {
    //     printf("%d", num);
    // } else {
    //     printf("%d", error_code);
    // }

    if (num == 2)
        printf("%d", num);
    else
        printf("%d", error_code);
}